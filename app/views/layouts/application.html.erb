<!DOCTYPE html>
<html lang="en">
<head>
  <title>Stock Rating</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div role="navigation" class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button data-target=".navbar-collapse" data-toggle="collapse" class="navbar-toggle" type="button">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <div class="navbar-brand"><span class="glyphicon glyphicon-stats"></span> Stock Rating</div>
      </div>
      <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav">

          <li class="dropdown <%= check_place(score_cards_path, request.fullpath)%>">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Score Cards <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'List', score_cards_path %></li>
              <li><%= link_to 'Add', new_score_card_path %></li>
            </ul>
          </li>

          <li class="dropdown <%= check_place(shares_path, request.fullpath)%>">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Shares <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'List', shares_path %></li>
              <li><%= link_to 'Add', new_share_path %></li>
            </ul>
          </li>

          <li class="dropdown <%= check_place(stock_indices_path, request.fullpath)%>">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Stock Indices <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'List', stock_indices_path %></li>
              <li><%= link_to 'Add', new_stock_index_path %></li>
            </ul>
          </li>

          <li class=<%= check_place(rising_scores_path, request.fullpath)%>><%= link_to 'Rising Price', rising_scores_path %></li>
          <li class=<%= check_place(deposit_entries_path, request.fullpath)%>><%= link_to 'Deposite', deposit_entries_path %></li>
        </ul>
        <% if current_user %>
	        <p class="navbar-text navbar-right">
	        	<span class="glyphicon glyphicon-user" data-toggle="tooltip" data-placement="bottom" title="<%= current_user.email %>" ></span>
	        	<%= link_to "Log out", log_out_path, :class => 'navbar-link' %>
	        </p>
        <% else %>
        	<p class="navbar-text navbar-right">
	        	<%= link_to "Log in", log_in_path, :class => 'navbar-link' %>
	        </p>
        <% end %>
      </div>
    </div>
  </div>
	<div id="content" class="container">
		<% if notice != nil %>
			<div class="info_box">
				<div class="center-block" style="width: 400px;">
				  <div class="alert alert-success alert-dismissable">
				  	<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
				  	<strong><%= notice %></strong>
				  </div>
				</div>
			</div>
		<% end %>
		<%= yield %>
	</div>
</body>
</html>
